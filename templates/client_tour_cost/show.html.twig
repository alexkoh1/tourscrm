{% extends 'base.html.twig' %}

{% block title %}ClientTourCost{% endblock %}

{% block body %}
    <h1>ClientTourCost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_tour_cost.id }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ client_tour_cost.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_tour_cost_index') }}">back to list</a>

    <a href="{{ path('client_tour_cost_edit', {'id': client_tour_cost.id}) }}">edit</a>

    {{ include('client_tour_cost/_delete_form.html.twig') }}
{% endblock %}
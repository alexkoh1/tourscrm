{% extends 'base.html.twig' %}

{% block title %}ExpenseType{% endblock %}

{% block body %}
    <h1>ExpenseType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expense_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expense_type_index') }}">back to list</a>

    <a href="{{ path('expense_type_edit', {'id': expense_type.id}) }}">edit</a>

    {{ include('expense_type/_delete_form.html.twig') }}
{% endblock %}
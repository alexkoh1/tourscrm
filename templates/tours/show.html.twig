{% extends 'base.html.twig' %}

{% block title %}Tours{% endblock %}

{% block body %}
    <h1>Tours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tour.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tour.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ tour.date ? tour.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Base_cost</th>
                <td>{{ tour.baseCost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tours_index') }}">back to list</a>

    <a href="{{ path('tours_edit', {'id': tour.id}) }}">edit</a>

    {{ include('tours/_delete_form.html.twig') }}
{% endblock %}